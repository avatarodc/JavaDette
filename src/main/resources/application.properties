#Active profile, defaults to collection if not set
#spring.profiles.active=${app.repository.type:jdbc}
# Uncomment this line to use JDBC profile
spring.profiles.active=collection

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/my_database
spring.datasource.username=papis
spring.datasource.password=Passer2020@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Optional server port configuration
server.port=8080
